/** Shared player capsule dimensions, reused by locomotion, collision, and camera code. */
export const PLAYER_CAPSULE_HEIGHT = 1.7;
export const PLAYER_RADIUS = 0.35;

/** Seconds for the sit/stand alignment transition (spec: "short alignment transition"). */
export const SEAT_TRANSITION_DURATION_SEC = 0.35;
